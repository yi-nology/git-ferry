services:
  git-ferry:
    build: .
    ports:
      - "8890:8890"
    volumes:
      - ./conf:/app/conf
      - git-sync-data:/app/data
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    # 不发布宿主端口:仅 git-ferry 内网可达,避免无密码 Redis 暴露宿主机
    expose:
      - "6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    restart: unless-stopped

volumes:
  git-sync-data:
  redis-data:
